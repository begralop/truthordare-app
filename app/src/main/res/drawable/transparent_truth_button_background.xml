<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/transparent" /> <!-- Color de fondo del botón -->
            <stroke android:width="8dp" android:color="@color/blue_btn" /> <!-- Ancho y color del borde (stroke) -->
            <corners android:radius="4dp" /> <!-- Borde redondeado, si es necesario -->
        </shape>
    </item>
</layer-list>
